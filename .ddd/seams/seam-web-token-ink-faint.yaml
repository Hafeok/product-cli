format: 1
id: seam/web/token-ink-faint
boundary: design token --ink-faint in ddd-core/assets/render.css
verdict_knowledge: The de-emphasis ink the muted class resolves to — the palette's floor; coverage and provenance prose reads at this level.
contract_location: ddd-core/assets/render.css#--ink-faint
obligations:
- governed by dec/web/render-status-palette via .ddd/manifest/tokens.yaml — ddd why resolves the chain
- a raw restatement of the value on a colour property is a stylelint finding (dec/web/token-discipline)
metadata:
  kind: token
  pair_counterpart: ''
  pair_direction: definition
  signature: color
  symbol: --ink-faint
  visibility: unlayered
