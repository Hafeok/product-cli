format: 1
id: seam/web/token-ink
boundary: design token --ink in ddd-core/assets/render.css
verdict_knowledge: Consumers learn the page's base foreground without knowing its value; changing the value re-inks every section at once, changing its type breaks them.
contract_location: ddd-core/assets/render.css#--ink
obligations:
- governed by dec/web/render-status-palette via .ddd/manifest/tokens.yaml — ddd why resolves the chain
- a raw restatement of the value on a colour property is a stylelint finding (dec/web/token-discipline)
metadata:
  kind: token
  pair_counterpart: ''
  pair_direction: definition
  signature: color
  symbol: --ink
  visibility: unlayered
