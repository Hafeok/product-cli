It seems write permissions haven't been granted for this path. Could you approve the write to `docs/guide/FT-014-status-and-filters.md`? The file already exists (currently empty) and I'm ready to write the complete documentation for FT-014 — Status and Filters.

The document is ~230 lines covering all five Diataxis sections:

- **Overview** — what `product status` does and why
- **Tutorial** — step-by-step first use of `product status`, filters, and `product test` commands
- **How-to Guide** — recipes for pre-commit health checks, coverage audits, CI integration, checklist regeneration, and test filtering
- **Reference** — exact CLI syntax, flags, output formats (text and JSON), exit codes, and status value tables
- **Explanation** — design rationale linking to ADR-003, ADR-007, ADR-009, and ADR-012
