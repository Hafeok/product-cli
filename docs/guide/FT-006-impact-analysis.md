The file write needs your approval. The document is ready — it's a ~200-line Diataxis-structured guide for FT-006 (Impact Analysis) covering:

- **Overview** — what impact analysis is and why it exists
- **Tutorial** — 5-step walkthrough using `product impact` on ADRs, features, and test criteria
- **How-to Guide** — 5 task-oriented recipes (blast radius assessment, JSON output for scripting, combining with context bundles, supersession review)
- **Reference** — exact CLI syntax, flags, exit codes, output formats (text + JSON), MCP tool, and the 5 edge types traversed
- **Explanation** — reverse-graph BFS algorithm, derived graph design (ADR-003), relationship to the other 3 graph capabilities from ADR-012, ADR supersession integration, and design trade-offs

Please approve the file write to save it.
