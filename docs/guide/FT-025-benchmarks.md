The file is ready to write — it's ~200 lines covering all five Diataxis sections. Here's a summary of what's included:

- **Overview** — what the benchmark suite is and why it exists
- **Tutorial** — 4 steps: build, run `cargo bench`, interpret output, investigate failures
- **How-to Guide** — 4 recipes: run benchmarks, run on Pi 5, check via `product verify`, add a new benchmark
- **Reference** — benchmark table with thresholds, exit codes, output format, file layout, feature metadata, LLM quality benchmarks (TC-030/031/032)
- **Explanation** — why fixed thresholds, why custom harness, why the delta threshold matters, relationship to ADR-012 graph algorithms

Could you grant write permission to `docs/guide/FT-025-benchmarks.md` so I can save it?
