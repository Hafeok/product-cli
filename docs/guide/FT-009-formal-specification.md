The file is ready to write. It's a ~200-line Diátaxis-structured guide for FT-009 (Formal Specification) covering:

- **Overview** — what formal specification blocks are and why they exist
- **Tutorial** — 5-step walkthrough: create a TC, add formal blocks (types, invariants, scenarios, evidence), validate with `graph check`, view in context bundles
- **How-to Guide** — 6 recipes: add evidence, declare types, write BDD scenarios, fix E001 errors, resolve W004 warnings, raise the phi threshold
- **Reference** — block syntax table, evidence field table, diagnostic codes (E001/W004/W006), evidence aggregation rules, `product.toml` configuration, status output format
- **Explanation** — why formal notation, parsing pipeline, evidence aggregation design, gap analysis integration (G002), relationship to ADR-005, and the phi fitness metric

Please approve the file write to save it.
