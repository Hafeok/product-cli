# Exemplar — the same single assertion in YAML form (the per-claim file
# pattern's literal shape). Present so the Gate 3 format ruling compares like
# with like; if YAML is ruled, CI gains a YAML->RDF conversion step at G0 and
# the Turtle exemplar is dropped.

format: registry-assertion/v1
id: assertion-0001
subject: Order
predicate: hasPart
object: OrderLine
reading:
  value: "Order hasPart OrderLine"
  as_of: 2026-08-17T00:00:00Z
  provenance: controlled        # controlled | observed | inferred | institutional (g-dec-01)
  assurance: "exemplar — hand-authored"
  # institutional provenance additionally requires:
  # trust_decision: <decision ref>
attributed_to: g1-session
