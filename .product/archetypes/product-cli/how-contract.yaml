# The self-hosted archetype reuses the repo's canonical How as its own — a
# single source of truth. Resolved by HowContract::load_opt (one ref hop,
# relative to this file). To make this a standalone archetype instead, replace
# this stub with an inline how-contract (see `product archetype init`).
ref: ../../how-contract.yaml
