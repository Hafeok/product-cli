# Invocation — rulings CG-R-81 … CG-R-84 and the second prediction, verbatim

Received 2026-09-14 mid-turn, after Emil's chat prediction (`prediction-emil-run6.md`) had been
filed and before either v5 inventory was walked. Filed as `rulings-gate1a-run6-predictions.md`
(sha256 in the commit that files it). The message:

> My prediction: A 87–94 centre 91; B 68–80 centre 74. B's centre sits one point under the §12.1
> line deliberately — it says the ratio's disposition turns on the repairs rather than on the
> codebase. Retiring factory-provider-by-return-type moves 231 edges into roles that must now
> actually resolve, and retiring data-contract returns 181 more, both entering a denominator they
> were classified out of by shapes that were hard to follow. If B lands above 80% I was wrong about
> how much those readings were hiding.
>
> The three predictions now differ on B's disposition, which is the useful shape: the session
> clears, I fire narrowly, and yours spans the line so it commits to neither. Recorded rather than
> corrected — a band is a legitimate form, and what isn't legitimate is discovering afterwards that
> nobody committed and then arguing about intent.
>
> CG-R-83 is the one to read before the run. registration-not-read's denominator membership isn't
> stated in what reached me and it moves A's headline a long way. Boundary is legitimately outside
> — nothing is chosen at composition. registration-not-read is the instrument's ignorance of a
> registration that exists, so excluding it means coverage rises as the table learns less, and a
> reader that parsed nothing would score perfectly.
>
> It goes inside the denominator — but from run 7, not now. Changing it after predictions are
> committed would be the fifth denominator move. Both figures get reported for run 6, labelled.
>
> One thing worth noticing in the Razor numbers. A's 45 directives against 91 composition edges is
> proportionally a worse blind spot than B's 342 against 1,450. A is the smaller, cleaner codebase
> and its invisible fraction is the larger.

**On the record's naming.** The rulings file is "issued by Emil" and gives the CG-R-81 prediction
as "mine"; CG-R-82 calls the chat prediction (A 80–100, B 70–95) "Emil's" in the third person.
The session records both as they arrived — the rulings author's prediction (CG-R-81) and the
chat prediction (filed first, `prediction-emil-run6.md`) — and does not resolve the naming.

**What it settles before the run.** CG-R-81: the rulings author's prediction, committed. CG-R-82:
the chat prediction's B band spans §12.1's line and is recorded as not committing on B's
disposition; the session's commits (clears), CG-R-81's commits (fires narrowly). CG-R-83:
`registration-not-read` is inside the denominator **from run 7**; run 6 reports both figures,
labelled, against the restated rule (outside); the restatement states membership explicitly.
CG-R-84: `ViewComponent` is a root in A, its two known edges leave the recall miss list.
