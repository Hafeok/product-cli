# Invocation — verbatim

The chat message that launched this session, reproduced exactly. The attachment it names is
filed under `inputs/` (see `arrived-inputs.md`). `<REF>` is reproduced as it arrived — the
placeholder was not substituted by the sender; see `bootstrap.md` for the ref actually read.

---

@"/root/.claude/uploads/5816e30b-ba90-5d06-945b-75e8aa4a18b8/f759a41f-csharp-binding-bundle.zip" Implement the C# stack binding in Hafeok/product-cli.
Attached: csharp-binding-bundle.zip. Read session-implement-csharp-binding.md — it is the instruction set. The PRD is at inputs/prd-csharp-stack-binding.md (f0049a22…) and the domain state change binding beside it.
Before Gate 0, read Hafeok/canon-governance at <REF> and record the ref. Its rules are in force and are not restated in the bundle.
Branch claude/csharp-stack-binding-<suffix>. Session-neutral identity Claude <noreply@anthropic.com>. Prompt and bootstrap to meta/sessions/ as the first act, before any code.
Five gates: orientation, the delta measure alone, declaration and profiles, the instrumented slice run, report. Hold at each. Propose; do not ratify your own output.
