# Invocation — rulings CG-R-68 … CG-R-70, verbatim

Received 2026-09-14 with `rulings-cg-r-68-70.md`, filed beside it as `rulings-gate1a-criterion.md`
(sha256 `1a42435ae9afae948d0848b0b0e7f56cd38631e143dbb63c8e1614163616f592`).

---

The message carried the rulings file alone. What it settles, in the order worked:

1. **CG-R-68** — the criterion is ratified with one amendment: a fourth state, `boundary`, for an
   edge to an external abstraction with no in-solution implementation, reported separately with
   the external type and the assembly, excluded from the resolved/unresolved denominator. Three
   points of operational form to state in the record: container-constructed from the registration
   list, not from shape; property and method injection are composition edges — state whether the
   reader emits them; CG-R-61's declaration shortcut does not apply (neither solution declares).
2. **CG-R-69** — runs 3 and 4 are instrument history, not measurements; nothing computed after the
   O-11 fix is compared with them. The reader's emission rules are fixed and stated before a
   prediction is committed. Emil's own hand estimate (about 78% for A) is corrected on the record.
3. **CG-R-70** — the prediction, committed against the amended criterion: A 88–96% (centre 92%),
   B 58–72% (centre 65%), with the expected unresolved distributions.
4. CG-R-67 discharged for `ddd diff-contracts`; the `run_all.sh` patch accepted in principle (Emil
   applies it in the binding repository); both v3 inventories are walked after the predictions are
   committed, not before. Register debt CG-R-17 … CG-R-70 is Emil's.

Recorded by the session; the rulings are Emil's.
