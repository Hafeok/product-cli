# Invocation — rulings CG-R-92 … CG-R-95, verbatim

Received 2026-09-14 as `rulings-cg-r-92-95.md`, filed as `rulings-gate1a-run7.md`; the message
otherwise repeated the session's run-7 summary back. What it settles, in the order worked:

1. **CG-R-92** — CG-R-91 failed on both for a property it did not consider: resolution expands
   the graph; **coverage is not monotone in resolver quality**; every cross-run comparison of
   coverage states this; the classified fraction is what moves monotonically.
2. **CG-R-93** — second convergence (classified 91.8% / 92.9%); **coverage leaves the headline
   entirely**, retained beneath the scored fraction and the error bound, never the figure quoted.
3. **CG-R-94** — *parsed* and *known* are two figures; the instrument record must not count
   itself; `IHtmlLocalizer<T>` behind a builder chain through a variable joins the limits list
   with its incidence (66 edges, dated).
4. **CG-R-95** — the table grows by measured frequency with a stated N, every entry justified by
   the edges it moves, its CG-R-79 coverage showing whether it worked.

Recorded by the session; the rulings are Emil's.
