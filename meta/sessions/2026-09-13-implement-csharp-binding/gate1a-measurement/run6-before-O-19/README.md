The two primary reports as first produced on 2026-09-14, before O-19 was repaired (the
registration-knowledge table's "unknown reached calls" counted Orchard's own extension methods —
types whose ids carry an `@assembly` suffix — as external: 256 of B's 380). Coverage figures are
identical before and after; only the CG-R-79 table line changes. Kept as instrument history.

The first pass was also produced by a binary built before the CG-R-83 second figure was added to
the render (the release build that should have carried it never ran — a broken command chain,
noticed on the diff); the second pass carries it. Nothing else differs.
