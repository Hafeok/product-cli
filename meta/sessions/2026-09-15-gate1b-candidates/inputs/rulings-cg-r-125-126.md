# CG-R-125 and CG-R-126 — Gate 1b closed

**Issued by Emil, 2026-09-15.**

---

## CG-R-125 — What Gate 1b established, and what it did not

**Closed as specified.** The corrections landed without moving a figure, the superseded statement stands with its correction appended rather than rewritten, and the session stopped where it said it would.

**Established.**

- **The entry-point candidate vocabulary works mechanically.** Recall 68 of 71, precision 68 of 68 — the fourth component to hold at 100% precision. CG-R-105's bet that a vocabulary could be derived without domain modelling is borne out at the derivation step.
- **Ratification is workable at this scale and produces things the derivation cannot.** Two merges that the overlap measure did not find and could not have, one deferral that found a real defect, and three rejections of page renders that carry no act.
- **The split fires §12.1, with the cause named and bounded.** 60.4 points against 21.9, and the dominant cause is a single subsystem: twelve of twenty acts carry no position and 26 of 29 unfollowed edges are the identity stack. **The instrument is blind to exactly the subsystem that carries most of A's undeclared edges.** That is actionable, bounded, and worth more than a clean number would have been.

**Not established.**

- Nothing about A's specification coverage. §12.1 firing means the split cannot be read, and CG-R-124 means the figure measures my subset as much as the codebase.
- Nothing about the identity half of A, which was not measured at all.
- Nothing about whether the flow works for anyone who did not design it.

**Two findings about A, and their weights are different.** F-EP-3 is real and small — a class name disagreeing with its file name, found by confirming a route row by row. F-EP-5 is not a finding about A at all: the ordering side reads as unstructured because I did not ratify an ordering entry point. Keeping them in one list under one heading would flatter the second.

**The weakest point is correctly stated and correctly attributed.** The one surviving statement about A — that the ordering side, manage pages and client are isolated — is the session's argument from where the unfollowed edges point, not the instrument's finding. Naming whose argument it is is what makes the result usable.

---

## CG-R-126 — Order of the outstanding work

**1. Gate 3, greenfield.** Bundled, unrun, and the test of the claim everything else supports: whether act, fact and position are sufficient for an actor to build a conforming slice without asking what was meant.

It goes first, and not only because it is cheap. **The flow's `implement` and `close` are the half that has dropped out of every sequence in this programme**, and they are the half Gate 3 exercises. Building the pipeline before knowing whether a slice can be built from a specification would be plumbing for an untested claim — and if Gate 3 says the specification is insufficient, `implement` has a different shape.

**2. CG-R-101's table ground truth**, with the `AddHttpClient<T>` row. It is the bound's first consumer and the last instrument task; CG-R-103 closes instrument work when it lands. It would let A's split be read, but only on a codebase nobody holds intent for, so it ranks below the claim test.

**3. The flow session**, toward a prototype. After Gate 3, with `implement` shaped by what Gate 3 found.

**4. The notation falsifier**, still held on the reconciliation, with the category list's provenance grade unassigned.

**Solution B stays out of scope**, as it has been throughout.

**The channel axis has its first real instance and no determination yet.** That is not a task; it is the thing to write when the first determination is authored against an accepted act — which is Gate 3's business or the flow's, not a gate of its own.

---

## The register

CG-R-17 through CG-R-124 live only in downstream verbatim filings. That is 108 rulings held outside the repository whose purpose is to hold them, and I have flagged it in almost every ruling set without discharging it.

**I will draft the rows on request and stop raising it otherwise.** Repeating a debt I cannot pay unilaterally adds nothing to a record that already states it.

---

Register debt: CG-R-17 … CG-R-126.
