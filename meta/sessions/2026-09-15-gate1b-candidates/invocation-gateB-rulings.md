# Invocation — rulings CG-R-121 … CG-R-124, verbatim

Received 2026-09-15 after Gate B was held: one attachment, no chat text. Filed as
`inputs/rulings-cg-r-121-124.md`, sha256 `f7ff2a950cf243d234603c022f2df6d4d7ff3f114c521a15317b2000375bb114`.

What it settles:

- **CG-R-121** both Gate B predictions failed on the bound for one cause; Emil's reachable band
  failed and the session's held. Reachability from a ratified subset propagates through shared
  infrastructure, not across sibling features.
- **CG-R-122** §12.1 **fires** in substance: the bound is read against the effect it qualifies,
  never against a constant; the residual numeric form (distance to ~90) is struck. Firing means
  the reachable/isolated split on A cannot be trusted as instrumented; the named cause is the
  unvalidated table (CG-R-101) plus P-EP-4's blindness.
- **CG-R-123** the bound's composition gains a third line — unfollowed edges with external
  targets — reported beneath the bound, never subtracted.
- **CG-R-124** every reachable/isolated figure states the subset it was computed from; the
  headline carries the subset's size and grade (`stand-in, 22 of 68 entry points`). Gate C
  leads with: the split measures the ratified subset as much as A; in substance, the
  non-identity half of A measured, the identity half not measured at all.
